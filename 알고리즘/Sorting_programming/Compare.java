package Sorting_programming;

public class Compare {				// 비교하는 값을 담을 클래스

	 int count=0; 					//비교 횟수 저장 변수
		
		public void compare() {
			count++;				// compare 함수 호출 되면 count+1
		}
		public int showcount() {	//count 값 리턴
			return count;
		}
	}

